var moment = require('moment');
import Resets from "./src/resets.json";

var resets = Resets;


resets.map( (reset) => {

    reset.title;
    reset.description;
    reset.when;

} )

module.exports = {
    

}